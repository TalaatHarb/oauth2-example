import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'oauth2-example-frontend',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
